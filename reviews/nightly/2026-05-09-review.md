# Nightly Review — 2026-05-09

Commit: `636560203c6b4711e11e03f553c32f7981e29918` — `update results.tsv with discarded pos_weight=20 experiment`

No code changes. Only `autoresearch/results.tsv` updated with three experiment rows (baseline + two discarded runs). A couple of housekeeping observations on the data / workflow:

- **Missing run identifier hygiene:** The `commit` column lists short SHAs (`3110fcb`, `79321cd`, `bca9acc`). Ambiguous once the repo grows. Switch to full 40-char or at least 12-char to prevent future collisions.
- **Inconsistent decimal precision:** `recall_50` and `position_mse` vary between 4 and 5 sig figs (e.g. `0.5909` vs `0.0087`). Standardise formatting (e.g. 4-decimal fixed) to make diffs cleaner and avoid spurious delta noise.
- **No automated append guard:** TSV is appended by hand. A script that validates schema + dedupes before writing would prevent double-logging if the commit is re-run.
